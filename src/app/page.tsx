import Search from "./components/Search";

/**
 * The root page of the app, which renders a search component.
 */
const Home = () => {
    return (
        <main>
            <Search />
        </main>
    );
};

export default Home;
